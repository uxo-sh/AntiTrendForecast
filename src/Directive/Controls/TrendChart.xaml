<UserControl x:Class="AntiTrendForecast.Directive.Controls.TrendChart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="400">
    <Grid Background="Transparent">
        <Border BorderBrush="#33FFFFFF" BorderThickness="0,0,0,1" VerticalAlignment="Bottom" Height="1"/>
        
        <Canvas x:Name="ChartCanvas" Margin="10,20,10,10" SizeChanged="Canvas_SizeChanged">
            <!-- Grid Lines (Horizontal) -->
            <Line X1="0" Y1="0" X2="{Binding ActualWidth, ElementName=ChartCanvas}" Y2="0" Stroke="#11FFFFFF" StrokeThickness="1" />
            <Line X1="0" Y1="{Binding Path=MidY}" X2="{Binding ActualWidth, ElementName=ChartCanvas}" Y2="{Binding Path=MidY}" Stroke="#11FFFFFF" StrokeThickness="1" />
            
            <!-- Trend Line -->
            <Polyline x:Name="TrendPolyline" 
                      Stroke="#00F2FF" 
                      StrokeThickness="3" 
                      StrokeLineJoin="Round"
                      StrokeStartLineCap="Round" 
                      StrokeEndLineCap="Round">
                <Polyline.Effect>
                    <DropShadowEffect BlurRadius="15" Color="#00F2FF" ShadowDepth="0" Opacity="0.6"/>
                </Polyline.Effect>
            </Polyline>
            
            <!-- Area Fill (Gradient) -->
            <Polygon x:Name="AreaPolygon" Opacity="0.1">
                <Polygon.Fill>
                    <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                        <GradientStop Color="#00F2FF" Offset="0"/>
                        <GradientStop Color="Transparent" Offset="1"/>
                    </LinearGradientBrush>
                </Polygon.Fill>
            </Polygon>
        </Canvas>
        
        <!-- Legend labels -->
        <TextBlock Text="100%" Foreground="#33FFFFFF" FontSize="10" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,5,5,0"/>
        <TextBlock Text="0%" Foreground="#33FFFFFF" FontSize="10" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="0,0,5,5"/>
    </Grid>
</UserControl>
