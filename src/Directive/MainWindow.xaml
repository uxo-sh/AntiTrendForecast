<Window
    x:Class="AntiTrendForecast.Directive.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Anti-Trend Forecast Engine">

    <Grid Padding="32" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="📉 Anti-Trend Forecast Engine"
                   Style="{StaticResource TitleTextBlockStyle}"
                   HorizontalAlignment="Center"/>

        <!-- Search Input -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12" HorizontalAlignment="Center">
            <TextBox x:Name="KeywordInput"
                     PlaceholderText="Enter a keyword (e.g., AI Agents)"
                     Width="350"/>
            <Button x:Name="AnalyzeButton"
                    Content="Analyze"
                    Style="{StaticResource AccentButtonStyle}"
                    Click="OnAnalyzeClicked"/>
        </StackPanel>

        <!-- Results Area -->
        <StackPanel Grid.Row="2" x:Name="ResultsPanel" Spacing="8" Padding="0,16,0,0">
            <TextBlock x:Name="StatusText"
                       Text="Enter a keyword above to begin analysis."
                       Style="{StaticResource BodyTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>
    </Grid>
</Window>
